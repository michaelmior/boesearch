# Password for the 'elastic' user (at least 6 characters)
ELASTIC_PASSWORD=56Gx%9A2yhOW

# Password for the 'kibana_system' user (at least 6 characters)
KIBANA_PASSWORD=t6k9Ts&mg6p9

# Version of Elastic products
STACK_VERSION=8.3.3

# Set the cluster name
CLUSTER_NAME=boesearch

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE=basic

# Port to expose Elasticsearch HTTP API to the host
ES_PORT=9200

# Port to expose Kibana to the host
KIBANA_PORT=5601

REACT_APP_ES_URL=https://${CODESPACE_NAME}-9200.githubpreview.dev

REACT_APP_ES_INDEX=boesearch

REACT_APP_ES_USER=frontend

REACT_APP_ES_PASSWORD=l1E69&80WmRR

ES_ALLOW_UNAUTHORIZED=true
